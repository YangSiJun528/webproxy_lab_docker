#include <stdio.h>

int main(int argc, char **argv)
{
  (void)argc;
  (void)argv;

  /*
   * TODO:
   * 1. 명령행 인자에서 연결할 host와 port를 읽는다.
   * 2. socket(), connect()로 TCP 서버에 연결한다.
   * 3. 서버로 보낼 데이터를 준비한다.
   * 4. 데이터를 write하고 echo 응답을 read한다.
   * 5. 연결 socket을 닫는다.
   */

  return 0;
}
