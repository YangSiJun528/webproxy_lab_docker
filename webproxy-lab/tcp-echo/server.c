#include <stdio.h>

int main(int argc, char **argv)
{
  (void)argc;
  (void)argv;

  /*
   * TODO:
   * 1. 명령행 인자에서 listen할 port를 읽는다.
   * 2. socket(), bind(), listen()으로 TCP 서버 socket을 준비한다.
   * 3. accept()로 클라이언트 연결을 받는다.
   * 4. 클라이언트가 보낸 데이터를 읽고 그대로 다시 write한다.
   * 5. 연결과 listening socket을 닫는다.
   */

  return 0;
}
